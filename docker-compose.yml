services:
  app:
    image: ghcr.io/${GITHUB_OWNER}/liyen:${IMAGE_TAG:-latest}
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - RESEND_API_KEY=${RESEND_API_KEY}
    networks:
      - proxy

networks:
  proxy:
    external: true
